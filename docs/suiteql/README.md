## SuiteQL & fallbacks REST

- **Stratégie générale**  
  - `SUITEQL_FALLBACK.md` : explique comment le serveur gère l’absence de permissions SuiteQL et les fallbacks REST correspondants.

- **Analyse API & couverture**  
  - `NETSUITE_API_ANALYSIS.md` : analyse détaillée des endpoints NetSuite utilisés.  
  - `NETSUITE_API_TEST_REPORT.md` : rapport de tests des endpoints (taux de succès, erreurs typiques).

Utilise ce dossier comme point d’entrée pour tout ce qui touche à SuiteQL (requêtes avancées) et aux stratégies de repli via l’API REST standard.

