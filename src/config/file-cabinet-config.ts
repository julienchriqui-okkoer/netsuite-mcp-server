/**
 * NetSuite File Cabinet folder for uploaded invoice PDFs.
 * Find in NetSuite: Documents → Files → Accounting → folder Internal ID.
 * Used by the "attach Spendesk PDF to vendor bill" flow.
 */
export const NS_FILE_CABINET_FOLDER_ID = "TO_CONFIRM";
